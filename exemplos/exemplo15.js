// | Exemplo 15 | Descrição |
// `Conversor de temperatura` | Crie uma função que recebe uma temperatura em graus celsius retorn
// a o valor convertido em Fahrenheit. |


function coverteTemperatura(valor) {
    return (valor * (9/5)) + 32
}

console.log(coverteTemperatura(0));