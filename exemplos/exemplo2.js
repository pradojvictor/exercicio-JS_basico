//Exemplo 02 | Descrição |
// `Concatenar x interpolar` | Vamos criar variaveis usando let, const e var,
//  atribuir strings e vamos concatenar e interpolar. |


var Nome = "victor"; 
console.log('concatenando usando var' + " " + Nome);

{
let Nome = "victor";
console.log(`interpolando usando let ${Nome}`);
}

{
const Nome = "victor"; 
console.log('usando const' + " " + Nome);
}